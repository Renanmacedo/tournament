import { createTheme } from './stitches.config'
 const tournament = createTheme('tournament', { 
    colors: { 
        primary: '#666bcb',
        primaryLight: '#9999ff',
        primaryDark: '#314099',
        primaryA100: '#ccd9ff',
        background: '#ffffff',
        boxColor: '#ffffff'
    },
    fonts: { }
})
export default tournament